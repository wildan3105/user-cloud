include ../static/layout

body
  div(class='container')
    div(class='row')
      div(class='col-md-10')
      h3 List of all participants : 
        strong #{n}
      p
      form(method='post')
        table(class='table table-striped')
          tr
          th No.
          th Email
          th Password
          th Registered at
          th Status Aktif
          th Edit
          th Delete
          th Activate
          each user in users
            tr
              td=user.index
              td=user.email
              td=user.password
              td=user.registered
              td=user.isActive
              td
                a(href='./list/#{user.email}', class='btn btn-primary', name='edit', value='Edit') Edit
              td
                a(href='./list/#{user.email}/delete', class='btn btn-danger', name='delete', value='Delete') Delete
              td
                a(href='#', class='btn btn-success', name='activate', value='Activate') Activate
      a(href="./home") Back to Dashboard        
    
  
